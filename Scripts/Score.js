// let score = 0;

// export const GetScore = () => {
//   return score;
// };

// export const SetScore = (value) => {
//   score += value;
// };

// function updateScore() {
//   scoreContext.clearRect(0, 0, scoreCanvas.width, scoreCanvas.height);
//   scoreContext.fillText(`Score: ${score}`, 10, 30);
// }
// const scoreCanvas = document.getElementById("score-canvas");
// const scoreContext = scoreCanvas.getContext("2d");

// scoreCanvas.style.background = "blue";

// var windows_Width = window.innerWidth;
// var windows_Height = window.innerHeight;

// //scoreCanvas.width = windows_Width;
// //scoreCanvas.height = windows_Height;

// scoreContext.fillStyle = "#3498db";
// scoreContext.font = "24px Arial";
// scoreContext.fillText = "fdsfds";
// scoreContext.globalAlpha = 0;
// scoreContext.fillRect(0, 0, 100, 100);
